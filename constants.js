const constants = (function () {
  'use strict';

  const ballSpeed = 9;
  const ballSize = 64;
  const halfBallSize = ballSize / 2;
  const circleSize = 778;

  return {
    ballSpeed,
    halfBallSize,
    circleSize,
    ballSize
  };
})();
