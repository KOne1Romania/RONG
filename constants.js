const constants = (function () {
  'use strict';

  var ballSpeed = 3;
  var ballSize = 64;
  var halfBallSize = ballSize / 2;
  var circleSize = 778;

  return {
    ballSpeed,
    halfBallSize,
    circleSize,
    ballSize
  };
})();
